import React,{useState}from'react';
import{motion,AnimatePresence}from'motion/react';
import{X,Mail,ShieldCheck,ArrowRight,CheckCircle2,AlertTriangle,RefreshCw}from'lucide-react';
interface Props{isOpen:boolean;onClose:()=>void;showToast:(msg:string,type?:'success'|'error'|'info')=>void;onSuccessLogin?:()=>void}
export default function ForgotPasswordModal({isOpen,onClose,showToast}:Props){const[email,setEmail]=useState(''),[busy,setBusy]=useState(false),[sent,setSent]=useState(false),[error,setError]=useState('');if(!isOpen)return null;const submit=async(e:React.FormEvent)=>{e.preventDefault();const value=email.trim().toLowerCase();if(!value.includes('@')){setError('Please enter a valid email address.');return}setError('');setBusy(true);try{const r=await fetch('/api/auth/password-reset/request',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email:value})});const d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(d.error||'Password recovery is temporarily unavailable.');setSent(true);showToast('If the account exists, a secure Axi Trades reset email has been sent.','success')}catch(err:any){setError(err?.message||'Password recovery is temporarily unavailable.');showToast(err?.message||'Password recovery is temporarily unavailable.','error')}finally{setBusy(false)}};return <AnimatePresence><div className="fixed inset-0 z-[90] flex items-center justify-center p-4 bg-slate-950/75 backdrop-blur-sm"><motion.div initial={{opacity:0,scale:.96}} animate={{opacity:1,scale:1}} exit={{opacity:0,scale:.96}} className="bg-white dark:bg-slate-900 w-full max-w-md rounded-2xl shadow-2xl overflow-hidden border border-slate-200 dark:border-slate-800"><div className="bg-slate-950 text-white p-5 flex items-center justify-between"><div className="flex items-center gap-3"><span className="text-2xl font-black text-[#E3000F]">axi</span><span className="text-xs font-bold uppercase tracking-wider text-slate-300 border-l border-slate-700 pl-3">Password Recovery</span></div><button onClick={onClose}><X className="w-5 h-5"/></button></div><div className="p-6">{sent?<div className="text-center py-5"><CheckCircle2 className="mx-auto w-14 h-14 text-emerald-500"/><h3 className="mt-4 text-lg font-bold">Check your email</h3><p className="mt-2 text-sm text-slate-500">If the account exists, we sent a secure Axi Trades password-reset link to <strong>{email}</strong>.</p><button onClick={()=>{setSent(false);setEmail('')}} className="mt-5 text-sm font-bold text-[#E3000F] flex items-center gap-2 mx-auto"><RefreshCw className="w-4 h-4"/>Use another email</button></div>:<form onSubmit={submit} className="space-y-5"><div><h3 className="text-lg font-bold">Forgot your password?</h3><p className="mt-1 text-sm text-slate-500">Enter the email registered with Axi Trades. We will send a secure reset link from Axi Trades Official.</p></div>{error&&<div className="bg-red-50 text-red-700 p-3 rounded-xl text-xs flex gap-2"><AlertTriangle className="w-4 h-4"/>{error}</div>}<div className="relative"><Mail className="absolute left-3 top-3 w-4 h-4 text-slate-400"/><input value={email} onChange={e=>setEmail(e.target.value)} type="email" autoComplete="email" className="w-full rounded-xl border border-slate-200 dark:border-slate-700 bg-slate-50 dark:bg-slate-800 pl-10 p-3" placeholder="you@example.com" required/></div><button disabled={busy} className="w-full rounded-xl bg-[#E3000F] text-white py-3 font-bold flex items-center justify-center gap-2 disabled:opacity-60">{busy?<RefreshCw className="w-4 h-4 animate-spin"/>:<><ShieldCheck className="w-4 h-4"/>Send secure reset link<ArrowRight className="w-4 h-4"/></>}</button></form>}</div></motion.div></div></AnimatePresence>}
